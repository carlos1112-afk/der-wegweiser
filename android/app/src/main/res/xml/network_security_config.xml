<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Prevent cleartext HTTP traffic to prevent network eavesdropping / data leaks -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
