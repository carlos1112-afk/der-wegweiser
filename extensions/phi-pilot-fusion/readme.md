# Phi Pilot Fusion

VS Code / AGY-IDE Extension. Ersetzt Gemini Code Assist durch Microsoft Phi-3 via HuggingFace.

## Build
```bash
cd extensions/phi-pilot-fusion
npm install && npm run compile
```

## Install (automatisiert)
Desktop-Skript ausführen: `install-phi-pilot-fusion.ps1`
