rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    // Charging Stations (Community-shared bike infrastructure)
    match /charging_stations/{stationId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if isAuthenticated();
    }

    // User Preferences
    match /user_preferences/{userId} {
      allow read, write: if true; // Fallback for demo & registered users
    }

    // User Memory Patterns
    match /user_memory_patterns/{userId} {
      allow read, write: if true;
    }

    // User Token Accounts & Gamification
    match /user_tokens/{userId} {
      allow read, write: if true;
    }

    // Saved Routes & GPX Tracks
    match /routes/{routeId} {
      allow read, write: if true;
    }

    // B2B Partner Leads & Charging Station Sponsors
    match /partner_leads/{leadId} {
      allow create: if true;
      allow read, write: if isAuthenticated();
    }

    // Default match-all for development & prototyping
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
